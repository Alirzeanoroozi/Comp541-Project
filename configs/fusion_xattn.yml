fusion:
  method: "cross_attention"
  dim: 256
  num_layers: 4
  num_heads: 8
  dropout: 0.1

training:
  epochs: 50
  batch_size: 4
  learning_rate: 2e-4
  optimizer: "adamw"
  scheduler: "cosine"
  loss: "cross_entropy"
  device: "cuda"
