fusion:
  method: "mil"
  projection_dim: 256
  mil_hidden: 512
  attention_heads: 4

training:
  epochs: 40
  batch_size: 4
  learning_rate: 1e-4
  optimizer: "adamw"
  scheduler: "linear"
  loss: "mse"
  device: "cuda"
